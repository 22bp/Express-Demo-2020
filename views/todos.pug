form(action='/todos', method='GET')
  input(name='q')
  button(type='subtmit') Search

form(action='/todos/create', method='POST')
  input(name='text')
  button(type='subtmit') Create

ul
  each todo in todos
    li #{todo.text}
  
  